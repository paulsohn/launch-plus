[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "launch-plus"
version = "0.1.0"
description = "Bazel-like build and run system for ROS 2"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [
    { name = "paulsohn" }
]
keywords = ["ros2", "launch", "build-system", "automation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Software Development :: Build Tools",
]
dependencies = [
    "click>=8.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4",
    "mypy>=1.0",
]

[project.scripts]
launch-plus = "launch_plus.cli:main"

[project.entry-points."ros2cli.command"]
launch-plus = "launch_plus.verb:LaunchPlusCommand"

[tool.maturin]
manifest-path = "crates/launch-plus-core/Cargo.toml"
features = ["python"]
python-source = "python"
module-name = "launch_plus._core"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
ignore = [
    "N806",   # uppercase variable names in functions (_SENTINEL, _TRANSITION_MAP) — module-level constants in nested scope
    "N812",   # import R alias in tests — idiomatic shorthand
    "E402",   # module-level imports not at top — py_resolver has required ordering
    "SIM105", # contextlib.suppress — many try/except/pass blocks have side effects before pass
]

[tool.ruff.lint.per-file-ignores]
"python/launch_plus/py_resolver.py" = ["E501"]  # long lines in comment/docstring — acceptable in data declarations

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.mypy]
python_version = "3.10"
# Gradual adoption: typed functions are checked, untyped functions are allowed.
# New code should have type hints; existing code is migrated incrementally.
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
check_untyped_defs = false
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_calls = false
