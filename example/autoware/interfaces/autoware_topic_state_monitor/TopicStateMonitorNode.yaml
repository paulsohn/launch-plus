# EXCEPTION: The monitored topic name and type come entirely from runtime params
# (params "topic" and "topic_type"). No static subscription topic can be listed.
# Only the diagnostic output is statically knowable.
connections:
  - name: /diagnostics
    type: publisher
    msg_type: diagnostic_msgs/msg/DiagnosticArray
